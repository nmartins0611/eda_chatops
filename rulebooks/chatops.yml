---
    - name: Listen for events on a webhook
      hosts: localhost
      sources:
        - ansible.eda.webhook:
            host: 0.0.0.0
            port: 5000

      rules:

#        - name: backup all switches
#          condition: event.payload.text is match("backup")
#          action:
#           run_playbook:
#            name: backup_all.yml
#            post_events: true

        - name: hello was said
          condition: event.payload.text is match("hello")
          action:
           run_job_template:
            name: Say_Hello
            organization: Default
            retries: 1
            delay: 10
